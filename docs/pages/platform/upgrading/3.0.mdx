---
meta:
  title: "Upgrading to 3.0"
  parentTitle: "Upgrading"
  description: "Guide to upgrade to Liveblocks version 3.0"
---

TODO: List of changes to document

- Removed deprecated things from
  [the 2.24 "notification settings"/"subscription settings" renames](https://liveblocks.io/docs/platform/upgrading/2.24)
  - Related, the `UPDATE_USER_NOTIFICATION_SETTINGS_ERROR` is now called
    `UPDATE_NOTIFICATION_SETTINGS_ERROR`, which was the previous name of
    `UPDATE_ROOM_SUBSCRIPTION_SETTINGS_ERROR`
- `LiveblocksUIConfig` → `LiveblocksUiConfig`
- Remove deprecated `RoomInfo` type from `@liveblocks/node`
  - We already mentioned this change as "breaking" in 2.0? With its own codemod
    https://liveblocks.io/docs/platform/upgrading/2.0#bc2
- Remove deprecated `useEditorStatus` hook from `@liveblocks/react-lexical`
  - TODO: Update/remove its docs section:
    https://liveblocks.io/docs/api-reference/liveblocks-react-lexical#useEditorStatus
- Remove deprecated `metadata` field from `getRooms` options in
  `@liveblocks/node`
- Remove deprecated `nextPage` property from `getRooms` response in
  `@liveblocks/node`
  - TODO: Should we also remove `nextPage` from the REST API docs?
- Remove deprecated `code` property from `LiveblocksError`
- Remove deprecated `unstable_fallbackToHTTP` client option
  - `largeMessageStrategy="experimental-fallback-to-http"` can be used instead
- Remove deprecated `events.storage` event source
  - TODO: `events` is marked as `@private`, so not a fully breaking change
- Remove deprecated `useStorageStatus` hook from `@liveblocks/react`
  - TODO: Update/remove its docs section:
    https://liveblocks.io/docs/api-reference/liveblocks-react#useStorageStatus
