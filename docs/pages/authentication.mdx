---
meta:
  title: "Authentication"
  description:
    "Understand permissions and choose between ID tokens and access tokens."
---

Authentication and permissions solve two different problems:

- **Authentication** confirms who the current user is (`userId`) and optionally
  which workspace they belong to (`organizationId`).
- **Permissions** define what a authenticated user can do with Liveblocks
  resources such as rooms, comments, and feeds.

## How permissions work

A user has access to a room through a list of permissions.

A permission has a left part, the resource (e.g. `*`, or `comments`) and the
right part, the scope (`read`, `write`, or `none`).

### Base permissions

By setting the permission for the resource `*`, you define the base scope for
all the resources of the room. You can choose between read or write access:

- `*:read` → the user will have read access to everything in the room.
- `*:write` → the user will have write access to everything in the room.

<Banner title="Legacy naming convention">

The legacy naming convention `room:read` and `room:write` are still supported,
they are equivalent to `*:read` and `*:write` respectively, but we recommend
using the new naming convention.

</Banner>

### More granular permissions

You can opt into or opt out of access to specific room resources:

- **Storage** with `storage:read`, `storage:write`, or `storage:none`.
- **Comments** with `comments:read`, `comments:write`, or `comments:none`.
- **Feeds** with `feeds:read`, `feeds:write`, or `feeds:none`.

**Here are some examples**

1. Write access to everything except read-only access to storage:

```
[
  "*:write",
  "storage:read"  // Lower storage access from write to read
]
```

2. Read access to everything, expect write acces to comments and no access to
   feeds:

```
[
  "*:read",
  "comments:write"  // Raise comments access to write
  "feeds:none"  // Remove access to feeds
]
```

## Authentication systems

Choose one of these approaches for generating auth tokens:

- [ID token authentication](/docs/authentication/id-token?source=authentication)
  (recommended): define permissions on rooms with Liveblocks APIs, then
  authenticate users with `identifyUser`.
- [Access token authentication](/docs/authentication/access-token?source=authentication):
  define allowed room IDs (often with wildcard patterns) every time you issue a
  token.

<Banner title="Use ID tokens for most production apps" type="warning">

ID tokens scale better for complex permission models (organization, group, and
user-level access). Access tokens are useful when you want to keep permission
logic entirely in your own auth endpoint.

</Banner>

<ListGrid columns={2}>
  <DocsCard
    title="ID token"
    href="/docs/authentication/id-token?source=authentication"
  />
  <DocsCard
    title="Access token"
    href="/docs/authentication/access-token?source=authentication"
  />
</ListGrid>
