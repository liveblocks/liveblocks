---
meta:
  title: "Playground"
  description: "Lorem ipsum"
---

<Banner title="Test" type="success">

Hello this is a `test`

</Banner>

# H1 `code` [#test-id]

Hello world

```jsx file="Cursor.jsx" isCollapsed isCollapsable
// CSS transition animated cursor
export default function Cursor({ color, x, y }) {
  return (
    <div
      style={{
        position: "absolute",
        top: "0",
        left: "0",
        transform: `translate(${x}px, ${y}px)`,
        transition: "transform 120ms linear",
      }}
    >
      <CursorSvg color={color} />
    </div>
  );
}
```

## H2 `code`

```jsx highlight="1"
const a = 2;
```

### H3 `code`

```jsx file="Cursor.jsx" isCollapsed isCollapsable highlight="4,6,8-10,12,14..16,18,20...23"
// CSS transition animated cursor
export default function Cursor({ color, x, y }) {
  return (
    <div
      style={{
        position: "absolute",
        top: "0",
        left: "0",
        transform: `translate(${x}px, ${y}px)`,
        transition: "transform 120ms linear",
      }}
    >
      <CursorSvg color={color} />
    </div>
  );
}
```

# H1 `code`

## H2 `code`

### H3 `code`

#### H4 `code`

##### H5 `code`

###### H6 `code`
