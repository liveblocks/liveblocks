{
  "name": "@liveblocks/examples-javascript-todo-list",
  "version": "1.0.0",
  "description": "This example shows how to build a collaborative to-do list without relying on a front-end framework—using esbuild for bundling.",
  "license": "Apache-2.0",
  "private": true,
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=static/app.js"
  },
  "devDependencies": {
    "esbuild": "^0.14.25"
  },
  "dependencies": {
    "@liveblocks/client": "^0.16.3"
  }
}
