{
  "name": "@liveblocks/javascript-todo-list",
  "description": "This example shows how to build a collaborative to-do list with Liveblocks and esbuild.",
  "license": "Apache-2.0",
  "private": true,
  "main": "app.js",
  "scripts": {
    "build": "esbuild app.ts --bundle --outfile=static/app.ts"
  },
  "devDependencies": {
    "esbuild": "^0.14.25",
    "prettier": "^2.6.2"
  },
  "dependencies": {
    "@liveblocks/client": "^0.19.1"
  }
}
