# https://liveblocks.io/dashboard/apikeys
LIVEBLOCKS_SECRET_KEY=

# https://liveblocks.io/dashboard/webhooks — one endpoint: /api/liveblocks-webhook
# (storageUpdated + commentCreated). Secret must match that endpoint.
LIVEBLOCKS_WEBHOOK_SECRET_KEY=

# https://platform.claude.com/settings/keys — AI assistant in comments + the "links" sparkle button
ANTHROPIC_API_KEY=

# https://typesafe.ai — Jev classifies priority, progress, assignee, and labels
# for the "properties" and "labels" sparkle buttons
TYPESAFE_API_KEY=

