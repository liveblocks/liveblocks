{
  "name": "@liveblocks/examples-express-javascript-live-cursors",
  "version": "1.0.0",
  "description": "This example shows how to build live cursors without relying on a front-end framework—using Express for authentication and esbuild for bundling.",
  "license": "Apache-2.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "esbuild app.js --bundle --outfile=static/app.js && node -r dotenv/config index.js"
  },
  "dependencies": {
    "@liveblocks/client": "^0.16.3",
    "@liveblocks/node": "^0.3.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "esbuild": "^0.12.2"
  }
}
