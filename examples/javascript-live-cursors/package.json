{
  "name": "@liveblocks/examples-javascript-live-cursors",
  "version": "1.0.0",
  "description": "This example shows how to build live cursors without relying on a front-end framework—using esbuild for bundling.",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "esbuild app.js --bundle --outfile=static/app.js"
  },
  "dependencies": {
    "@liveblocks/client": "^0.16.3"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "esbuild": "^0.12.2",
    "prettier": "^2.6.2"
  }
}
