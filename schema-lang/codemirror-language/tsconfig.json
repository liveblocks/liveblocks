{
  // Use the same settings for all packages
  "extends": "../../shared/tsconfig.common.json",
  "compilerOptions": {
    // To enable the custom loader for `syntax.grammar` files
    "allowArbitraryExtensions": true,

    // To work around Jest issues. Consider removing this setting again after switching to Vitest.
    "allowJs": true
  },
  "include": ["src", "plugins"]
}
