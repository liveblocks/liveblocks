{
  "name": "@liveblocks/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "tsup": "^6.2.3",
    "turbo": "^1.5.5",
    "typescript": "^4.7.4"
  }
}
