/* eslint-disable */
// @ts-nocheck
import type { RoomInfo, RoomUser } from "@liveblocks/node";

interface Settings {
  largestRoom: RoomInfo | null;
  rooms?: RoomInfo[];
}

const paginatedRooms: RoomInfo[][] = [];

function duplicateRoom(room: RoomInfo): RoomInfo[] {
  return data;
}
