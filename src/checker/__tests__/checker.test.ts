// import type { Document } from "../../ast";
// import { checkWithContext, buildContext } from "../";
// import { ErrorReporter } from "../../lib/error-reporting";
// import { parseDocument } from "../../parser/__tests__/helpers";

// function check(programText: string): Document {
//   const context = buildContext(ErrorReporter.fromText(programText));
//   const node = parseDocument(programText);
//   checkWithContext(node, context);
//   return node;
// }

// describe("typechecker", () => {
//   it("basics", () => {
//     check("x = 42").definitions[0];
//   });
// });

export {}; // Module cannot be empty
