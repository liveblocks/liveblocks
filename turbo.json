{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": [".envrc"],
  "globalEnv": ["CI", "LIVEBLOCKS_*", "NEXT_PUBLIC_*"],
  "tasks": {
    "dev": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**",
        "*.config.ts",
        "*.config.cts",
        "*.config.js",
        "*.config.cjs"
      ],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": ["jest.*", "vitest.*", "src/**", "test/**"]
    },
    "test:ci": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "inputs": ["jest.*", "vitest.*", "src/**", "test/**"]
    },
    "test:types": {
      "dependsOn": ["build"],
      "inputs": ["test-d/**", "vitest.*"]
    },
    "test:deps": {
      // A package's `test:deps` script has no dependencies and can be run
      // whenever. It also has no filesystem outputs.
      "outputs": []
    },
    "typecheck": {
      // Type-checks everything in the package, including unit tests, which the
      // `build` (tsup) and `test:types` scripts do not cover.
      "dependsOn": ["^build"],
      "inputs": ["src/**", "e2e/**", "test-d/**", "tsconfig.json"],
      "outputs": []
    },
    "lint": {
      // A package's `lint` script has no dependencies and can be run whenever.
      // It also has no filesystem outputs.
      "outputs": []
    },
    "lint:package": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "format": {
      "cache": false,
      "outputs": []
    }
  }
}
